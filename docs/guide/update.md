# 更新日志

## 2026 年 9 月

### 2026/09/08

<Badge type="tip" text="文档补充" />

- **等级筛选 B50 说明** — 补充 `13+b50`、`紫13+b50`、`14.0b50` 等按等级/定数筛选 B50 的用法，并说明 `ab50` 会取消 B35/B15 分组后直接取筛选结果中 Rating 最高的 50 首。
- **“单曲目等级 B50”释义** — 明确该功能是从用户自己的 B50 中筛选指定等级或定数的成绩，不是查询一首歌，也不是列出该等级的全曲库。

## 2026 年 8 月

### 2026/08/10
AWMC 版本号 `V26.10.1` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **AWMC NET 免绑定查分** — 新增默认成绩库 [AWMC NET.](https://net.wmc.pub)，新用户默认数据源为 AWMCNET；发送一次 `SGWCMAID` 即自动建档并同步成绩，无需绑定水鱼/落雪即可查询 B50；首次同步提示可用 QQ 邮箱注册 NET.，可用 `数据源 AWMCNET/水鱼/落雪` 切换。
- **成绩趋势** — 新增 `成绩趋势` / `rating趋势` / `mai趋势` / `awmc趋势 [天数]`，读取 AWMC NET. 服务端每日 Rating 趋势，无需开启本地数据存储。
- **卡密系统** — 新增 `兑换卡密` / `redeem` 兑换 BREAK / 双倍 / FREEDOM 卡密，支持群内与私聊自动兑换；`我的卡密` / `卡密状态` 查看当前生效加成；管理员可创建、查询、作废、统计卡密。
- **你想我猜（20 问猜曲）** — 新增 `你想我猜` / `20问猜曲`，Milk 心里想一首曲目，群友用「是/否」提问缩小范围并猜曲名；LLM 语义理解，支持分类/谱师/版本/定数等比较题、中途猜曲名、`查看已有信息` / `20问进度` 与 `重置你想我猜`；与其它猜歌玩法同群互斥。
- **账号只读查询** — 新增 `mai预览` / `mai道具`（成功查询各 5 BREAK），以及 `mai门状态` / `mai查门` / `查门`（5 BREAK），只读展示账号预览、道具列表与 Kaleidx Gate 的发现、钥匙、通关状态。
- **成绩与道具管理** — 新增交互式及一步式 `mai改成绩` / `改分`（75 BREAK / 条）、`mai删成绩` / `删分`（50 BREAK / 条），支持曲名/别名解析、难度选择、百分比达成率，按 DX 星级或实际分自动判断简单/专业模式；新增确认式 `mai清票` / `清票`（10 BREAK）清空 Charge 票券、`mai改道具` / `改道具`（100 BREAK，高风险、未经测试）。
- **BREAK 红包** — 新增 `发红包 [总额] [份数]` 群内手气红包、`抢红包` 领取、`红包状态` 查看领取明细、`收回红包`（满 90 秒，未领余额退回）；10 分钟未领金额自动退回。
- **补签与经济统计** — 新增 `AWMC补签` / `补签`，补昨天签到，每月最多 3 次，依次消耗 30 / 60 / 90 BREAK；新增 `BREAK统计` / `BREAK收支` 查看今日、近 7 天、近 30 天全局收支；`BREAK抽奖 帮助` 查看奖池概率；`贡献总榜` / `首富榜` 查看倾家荡产总贡献。
- **公告与门攻略** — 新增 `公告列表`、`确认阅读公告`；发送蓝/白/紫/黑/黄/红门名或 `哪些门`、`门攻略 [门名]` 获取对应 Kaleidx 门攻略链接。
- **其它指令** — 新增 `谱面排行` / `谱面热度`、`友人对战排行`、`不同意共享我的数据` / `同意共享我的数据` / `数据共享状态`。

<Badge type="warning" text="优化" />

- **AWMC Gateway API v2** — 适配新响应信封；发票改为同步执行并沿用近期真实耗时预计，移除已失效的发票队列查询与 `maiqueue` 命令。
- **发票价格** — 调整为倍率 × 10 BREAK（2 倍 20、3 倍 30、5 倍 50），旧默认倍率单价启动时自动迁移；默认仅允许 2 / 3 / 5 倍。
- **锐评计费** — `锐评一下` 改为按模型实际 Token 计费，调用前预扣 10 BREAK，成功后多退少补、生成或制图失败全额退回；同段结论补充脱敏聚合样本人数、谱面覆盖率与置信度，推分候选按 B35/B15 地板估算单曲收益。
- **签到经济收敛** — 连签奖励封顶 3，群倍数不再放大连签奖励；修复 MySQL 下 `break_config` 缺主键导致每次启动重复插入、读到历史无上限曲线的问题，管理员 WebUI 配置现在可真正生效。
- **直发二维码** — 即时提示撤回状态与动态预计耗时；无账号记录时自动验真绑定/认领，随后同步 PC 并按已绑定的水鱼/落雪渠道自动上传；明确 `maiu` 仅水鱼、`maiul` 仅落雪、`maiua` 两边，直发二维码默认走自动渠道。
- **我的 AWMC** — 改为分段聊天记录并加入现代化图片渲染，展示账号功能今日/累计/成功失败统计与最近流水。
- **图片渲染现代化** — 群 Rating 排行榜、我在群里有多菜、牌子进度、B50 风险面板、吃分推荐改为带曲绘的紧凑卡片；修复中文字体豆腐块与头像缓存。
- **二维码识别** — 图片二维码扫描隔离到带并发与 4 秒超时的子进程，新增原图 → 放大 → 灰度 autocontrast → 锐化多路兜底与多种二值化，手机拍屏/群图压缩识别率明显提升。
- **舞萌状态** — `舞萌状态` / `mais` 失败率改用 AWMC 全局统计接口，按业务错误、服务端/转发错误、客户端 4xx 分类绘图。
- **完成表缓存校验** — 定数表与完成表生成时记录曲目及定数签名，曲库变化后拒绝使用过期底图并提示管理员执行 `更新完成表`。
- **高负载附加费** — 60 秒内真实功能请求超过 30 个后，第 31 个起由触发者额外支付 1 BREAK（阈值、窗口、价格均可通过 ENV 调整）。
- 今日舞萌 BREAK 产出减半（人品值 ÷20）；查歌使用 WMC 标签替代退役 combined-tags。

<Badge type="danger" text="修复" />

- 修复开字母三字符错字容忍导致 `the` 误开曲名、别名均不含 `the` 的歌曲。
- 修复含金量/含水量出图头像不显示、今日吃分推荐无候选时 `ValueError`。
- 修复弱项处方标签与渲染阻塞、B50 风险预警图片编码、群 Rating 排行榜 MySQL 访问问题。
- 修复 `我的AWMC` 在 MySQL NULL 字段与图片二次编码下的报错。
- 修复卡密 MySQL 建表兼容、大 QQ 号自动兑换失败、多人同时兑换卡死事件循环。
- 修复你想我猜多处 LLM 提示词花括号、定数档位语义与结算卡死问题。

### 2026/08/06
AWMC 版本号 `V26.8.14` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **极限二选一** — 新增 `舞萌极限二选一` / `极限二选一` / `二选一`，支持 `加入` / `参赛` 参加五轮左右曲目比较；揭晓图明确标注绿 Basic、黄 Advanced、红 Expert、紫 Master、白 Re:MASTER 难度。
- **开字母看板模式** — 新增 `开字母文字模式`、`开字母图片模式`、`开字母自动模式`，可按群聊人数和负载选择渲染方式。

<Badge type="warning" text="优化" />

- **小游戏并发互斥** — 猜歌、猜曲绘、猜曲子、猜铺面、猜 Rating、找内鬼、极限二选一和开字母统一按群串行，避免重复开局与旧对局误结算新对局。
- **渲染性能** — 图片任务使用独立 CPU 线程池并提高默认并发额度，降低绘图任务对 Bot 消息响应的影响。
- **SW-API 配额提示** — `quota_exceeded` 会展示个人读/写配额、当前用量和北京时间恢复时间；配额耗尽不会被误报为二维码失效或验证失败。

## 2026 年 7 月

### 2026/07/28
AWMC 版本号 `V26.8.13` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **猜 Rating 四档难度** — `猜Rating` 随机 1～4 级，也可用 `猜Rating1`～`猜Rating4` 指定；难度越高，展示卡片和成绩辅助信息越少，积分与 BREAK 小幅提高；被抽中的题主不能作答或参与奖励
- **B50 找内鬼** — 使用 `找内鬼` / `找假卡` 开局，在 5 张 B50 卡片中找出单曲 RA 被篡改的一张；45 秒内发送 1～5 作答，按正确答案提交速度结算积分与 BREAK
- **我的猜歌七玩法** — 统计新增"猜 Rating"和"找内鬼"，趋势、玩法卡片、占比环图与近期明细改为按玩法数量自适应排版
- **倾家荡产** — 全部 BREAK 梭哈抽奖，支持标准 / 刺激 / 高风险三种模式；每次开局收取 2 / 3 / 5 BREAK 入场费（不退还），谢谢参与 70% / 78% / 82%，最高倍率 50x / 100x / 100x；随时可用（无日期限制）；输光的 BREAK 进入福利池（50% 可分配）
- **抽奖池** / `领取福利` — 查看今日贡献榜与领取福利，按贡献比例分配奖池

<Badge type="warning" text="优化" />

- **锐评计费与失败保护** — 现有 Token 基础价乘以 3；调用模型前预扣 6 BREAK，成功后按实际用量多退少补，模型或制图失败全额退回
- **锐评证据与推分路线** — 同段结论展示脱敏聚合玩家数、谱面覆盖率和置信度；推分候选按 B35/B15 对应地板估算单曲收益，并使用稳定排序方便复核
- **完成表缓存校验** — 生成完成表时记录曲目与定数签名；曲库变化后不再使用过期底图，并在启动或查询时提示管理员执行 `更新完成表`
- 猜 Rating、B50 找内鬼、开字母及其它猜歌玩法统一同群互斥；Bot 关闭时会清理未完成状态

### 2026/07/27
AWMC 版本号 `V26.8.12` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **猜 Rating 游戏** — 猜 Rating 新模式：随机选群友隐藏 B50 身份与成绩（只留曲绘 / 等级 / FC / FS），限时猜最接近 Rating；支持自定义首数与时间（`猜rating 30 90`）；固定积分 + BREAK 奖励（🥇15/3、🥈5/1、🥉3/0、参与1/0）

<Badge type="warning" text="优化" />

- 新增谱面标签 API `GET /api/v1/charts/:chartKey/tags`，返回难度分类、雷达轴 / 评估轴标签、回归特征贡献与谱面模式检测

### 2026/07/26
<Badge type="tip" text="功能追加" />

- **段位表** — 新增 PRiSM PLUS 段位认定课题图，指令 `段位表 真二段` / `真二段段位表`；支持 `@用户` 查询个人成绩展示
- **段位样本统计** — 图片展示 LIFE 规则、课题曲等级/定数、个人最高达成率、匿名样本量、均值、中位数、P25-P75 分布与 SSS 率

<Badge type="warning" text="优化" />

- 段位图使用玩家名牌与官方段位 Plate 资源，页脚标明实时/内置样本来源，并提示段位结果以机台为准

### 2026/07/20
AWMC 版本号 `V26.8.7` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **舞萌开字母计时结算** — 局内不计分/BREAK；全部解开后按用时星级 + 贡献分配积分与 BREAK；用时展示 `xx.xxx秒`
- **自适应星级** — 默认 ≤30/45/60/90/180 秒；群通关变快后按历史 P35 收紧五星（15–30 秒），其余星按比例缩放；结算展示本局阈值
- **结算展示** — 先短文案（用时 `xx.xxx秒` + 星级 + 奖池），再本局贡献分成图，最后全开看板；`不玩了` 仅揭晓不发速度/贡献奖
- **开字母限时×3** — 活动一周内开字母结算分数与 BREAK ×3，文案与榜图标注「限时×3」；过期自动恢复 1 倍
- **开字母榜单** — `开字母排行` / `开字母贡献榜` / `开字母时间榜`
- **我的猜歌** / `猜歌数据` / `猜歌统计` — 个人五模式（猜歌 / 曲绘 / 曲子 / 铺面 / 开字母）近 30 日趋势、积分/次数双雷达、模式卡片与近期明细；支持 `@某人` 查对方本群数据；群内 `我的AWMC` 自动附带猜歌数据图
- **舞萌状态** / `mais` — 「服务器失败率」折线图（账号操作全量：发票、maiu 上传、绑定等；`returnCode=0` 计失败；无数据时段省略）+ 服务器实时状态
- **猜铺面 BGM** — 曲末阶段预制谱面视频并混流 BGM；渲染与在线服隔离，避免拖垮接消息

<Badge type="warning" text="优化" />

- **猜歌答题限流** — 同一用户全局 2.5 秒只能答一次（猜歌 / 曲绘 / 曲子 / 铺面）；超限提示「嘿嘿，你的答案被我吃掉啦！」
- **开字母冷却** — 非高峰另有 2.5 秒/人冷却（与猜歌全局限频独立，文案同上）；人多/突发进入文字模式后跳过冷却
- **开字母人多文字模式** — 贡献人数或短时突发达阈值时，局中看板降级为纯文字；通关结算仍强制出图
- **猜铺面自适应并发** — 按系统负载调节预制/补洞并发，忙时降档保在线响应
- 统一存储 MySQL 同步加速：工作集未变更可跳过打包；未改文件可复用；启动同步后台执行、不阻塞接消息
- `我的AWMC` / `发票统计`（管理员）展示发票 `returnCode=0` 与 null/未返回次数

<Badge type="danger" text="修复" />

- 猜歌数据图排版：趋势图例与标题分行、雷达标签外推，避免遮挡

::: tip 文档
指令说明见 [指令帮助 · 猜歌](/guide/bot/advanced#26-猜歌) 与 [账号帮助](/guide/bot/advanced#11-帮助与信息)。
:::

### 2026/07/18
<Badge type="warning" text="文档 / API" />

- **AWMC 公共 API 网关换新上游**：开放路径改为 `/v1/health`、`/v1/user/*`、`/v1/charge`、`/v1/charge/queue`、`/v1/update-lx`、`/v1/update-fish`
- `keychip` 由服务端注入；调用方只需传 `qrcode` 等业务字段
- 计费以 HTTP 2xx 且上游 `code === 0` 为准；充值入队后绑定 mai `userId`，队列仅返回本人任务并脱敏
- 旧路径（如 `/v1/get_preview`、`/v1/upload_b50`、手动成绩类等）已下线，详见 [开发者文档](/dev/awmc-api)

### 2026/07/16
AWMC 版本号 `V26.8.5` [AWMC V2]

<Badge type="tip" text="功能追加" />

- 直接在群内发送二维码文本，即可自动同步
- 支持直接发送二维码照片，识别后自动同步

::: tip 修改建议
欢迎前往 [AWMC 论坛](https://bbs.wmc.pub/forums/awmc.19/) 提出建议。接入 AI 后会自动修改机器人，审核通过即可使用并更新。
:::

### 2026/07/15
AWMC 版本号 `RELEASE-20260715 V26.8.1` [AWMC V2]

<Badge type="tip" text="功能追加" />

- `maibindfish` / `mai绑定水鱼` 新增无参数交互式绑定：Bot 会提供水鱼查分器入口，并引导用户在“编辑个人资料”中生成和复制 `Import-Token`
- 水鱼 Token 格式错误时会继续等待，本轮最多可输入 3 次；支持发送“取消”退出

<Badge type="warning" text="优化" />

- 用户提交的水鱼 Token 会优先撤回；撤回失败时提示用户手动撤回
- 水鱼绑定成功仅显示脱敏 Token，并附带可供管理员追踪的 `REF_ID`
- 水鱼绑定提示、重试与结果会引用触发消息，降低多人同时操作时的歧义

<Badge type="danger" text="修复" />

- 修复发票实际成功后被错误提示为 `FinishedException()` 的问题
- 一并修复账号、地图、队列、落雪 B50 等流程中的同类成功结果误报

### 2026/07/05
<Badge type="tip" text="功能追加" />

- **maiBot 升级 V3**
- **官方机器人支持**
- **mai-queue** 已连接 [nearcade.com](https://nearcade.com)
- 增加 **b50 查询**体验

<Badge type="warning" text="优化" />

- 下载站去除群内验证，改为排队
- 因受水鱼影响，采用积分制度限制用户调用

<Badge type="danger" text="修复" />

- 下载站已知问题修复
- 修复索引无法下载的问题

## 2026 年 6 月

### 2026/06/30
<Badge type="tip" text="功能追加" />

- **游玩排行50**（`游玩PC50` / `PC游玩50` / `pc游玩50`）— 全库游玩最多的 50 首谱面图，不限于 Rating B50

<Badge type="warning" text="优化" />

- `pc排行` 标题改为「PC全部排行（共 N 人）」，展示全部已同步用户，不再误标为群内且不限前 15 人；新增别名 `pc全部排行` / `PC全部排行`
- `更新pc数` 完成提示、`pca50` 说明文案调整，与 `游玩排行50` 区分

### 2026/06/23
AWMC 版本号 `PRE-20260623 V26.6.4` [AWMC V2]

<Badge type="tip" text="功能追加" />

- **猜曲子** — 全新群小游戏：4 个阶段、每阶段 30 秒语音，乐器逐步叠加（鼓 → 鼓+贝斯 → +其他 → +人声）
- 越早猜中积分越高（10 / 9 / 7 / 5）
- 超级用户私聊可 `更新猜曲音频`；`更新猜曲音频 -full` 强制重建缓存

<Badge type="warning" text="优化" />

- 猜曲音频烘焙：先裁切再分轨防内存溢出，demucs 失败自动回退 ffmpeg；支持逐首异步烘焙与中断
- 猜曲全流程 `[GuessAudio]` 日志；缓存版本号自动作废旧数据
- 猜歌积分：日 / 周 / 月 / 季 / 赛季榜；递减计分；周期榜结算归档与历史查询；结算分条发送；连击与宴会场过滤
- B15 上分推荐：版本判定更新至 DX2026「丸」代曲目；曲库无 CiRCLE 时回退「镜彩」代；上分为空时多重回退
- PC 数拉取迁移至 sw-api `user/music` 接口

<Badge type="danger" text="修复" />

- 猜曲子四阶段混音与阶段文件校验
- 猜歌答案容错正则字符类
- B15 上分推荐多处筛选缺陷
- sw-api 返回 `code=0` 且 `msg` 为 JSON 时的解析

### 2026/06/15
<Badge type="tip" text="功能追加" />

- **猜歌积分排行** / **猜歌积分周榜** — 查看本群猜歌/猜曲绘总积分及本周排行（合并转发聊天记录形式）
- **弱项处方**（`弱项处方单` / `底力处方` / `练习推荐`）— 弱项处方单图片，支持 @他人
- **b50风险**（`B50风险` / `b50风险预警` / `风险预警`）— B50 风险预警图片，支持 @他人
- **对战战绩**（`headtohead` / `h2h` / `对决战绩`）— 与群友 Head-to-Head 对比图
- **目标rating**（`rating沙盘` / `目标分` / `推分沙盘`）— 推分沙盘文字方案，如 `目标rating 16000`

<Badge type="warning" text="优化" />

- 进阶教程文档同步上述新指令说明

### 2026/06/13
AWMC 版本号 `PRE-20260613 V26.6.3` [AWMC V2]

<Badge type="tip" text="功能追加" />

- 下载站 https://download.awmc.cc 上线
- 舞萌定数更新
- 群人数上限增加到 3000 人

<Badge type="danger" text="修复" />

- 修复下载站的若干问题
- 修复部分完成表显示问题
- 防止用户不刷新网页的东西

### 2026/06/10
<Badge type="tip" text="功能追加" />

- 新项目 download.awmc.cc 下载站上线
- 更新舞萌 DX 2026

<Badge type="warning" text="优化" />

- 迁移了很多数据到新的服务器

<Badge type="danger" text="修复" />

- 修复已知问题

### 2026/06/04
<Badge type="tip" text="功能追加" />

- 新项目上线：舞萌 DX 谱面下载站

<Badge type="warning" text="优化" />

- 优化成绩获取，新增缓存

<Badge type="danger" text="修复" />

- 修复批量下载可能造成的问题
- 修复 .adx 格式会额外添加一个 .zip

[v26.6.1 - Pre]

### 2026/06/01
AWMC 版本号 `ALPHA-20260601 V26.6.0.67` [AWMC V2]

<Badge type="danger" text="修复" />

- 修复已知问题

AWMC 版本号 `ALPHA-20260601 V26.6.0.65-66` [AWMC V2]

<Badge type="danger" text="修复" />

- 修复等级进度兼容未知同步类型（如 `fs=sync`），`_parse_level_plan` 同时支持 `combo_rank`/`sync_rank` 等内部值
- `舞舞` 改为等价 `fdx`（FSD/FDX 及以上）

AWMC 版本号 `ALPHA-20260601 V26.6.0.64` [AWMC V2]

<Badge type="tip" text="功能追加" />

- 支持中文简称进度查询：`13将`（sss）、`14+极`（fc）、`13神`（ap）、`13舞舞`（fsd）、`13者`（bbb），支持分页筛选和 @他人查询

AWMC 版本号 `ALPHA-20260601 V26.6.0.63` [AWMC V2]

<Badge type="tip" text="功能追加" />

- 新增 `地板` 查询指令 — 查看 B35/B15 地板，支持按难度/定数筛选、查他人，别名 `b50地板`/`rating地板`

AWMC 版本号 `ALPHA-20260601 V26.6.0.62` [AWMC V2]

<Badge type="warning" text="优化" />

- 日报/周报/月报/存档对比统一视觉重写（B50 背景、毛玻璃面板、B50 提升布局修复、页脚栏）

AWMC 版本号 `ALPHA-20260601 V26.6.0.61` [AWMC V2]

<Badge type="danger" text="修复" />

- 修复 B50 与游玩详情 DX 星数计算偏差 — `dxScore()` 改为浮点百分比直接比较阈值，不再 `int()` 截断

## 2026 年 5 月

### 2026/05/31
AWMC 版本号 `ALPHA-20260531 V26.6.0.50-26.6.0.60` [AWMC V2]
<Badge type="tip" text="功能追加" />

- **落雪（Lxns）数据源接入** — OAuth 绑定（`lxbind`）、数据源切换（水鱼/落雪）、`lxb50` 查询，数据源自动路由，曲库/谱面/别名本地缓存（1h）
- **成绩图页脚信息** — 所有成绩图下方显示数据源、主题、⏱️ 耗时（数据获取/图片渲染/总计），底部追加 `Generated by xxx BOT | QQ Group 1072033605`
- **B50 数据异常提示** — 水鱼无数据/掩码成绩/落雪无数据等场景自动诊断提醒

<Badge type="warning" text="优化" />

- **定数表/完成表渲染重写** — 统一网格坐标、毛玻璃卡片样式，舞/霸者完成表分页
- **底力分析图优化** — 毛玻璃面板、条形图白底轨道+数值徽章，雷达图样式优化
- **主题系统与图片路径统一** — `pic()`/`resolve_theme_path` 统一管理，字体路径改为 `static/font/`，图片缺失优雅降级

<Badge type="danger" text="修复" />

- 修复定数表/完成表 SSS 评级、FC/AP 图标错位
- 修复牌子图路径错误（`plate/` → `plate_version/`）
- 修复 lv15 定数表、舞系完成表崩溃（RaMusic 无 `basic_info`、不可哈希）
- 修复 `minfo`/`info` 渲染坐标及 TOTAL 列重叠
- 宴谱过滤：ID ≥ 100000 判定，避免宴谱混入 b50
- 移除白名单、上传水鱼/落雪、拿票等功能（按需精简）
- 启动钩子健壮性、谱面预览域名改为 `v.awmc.cc`

AWMC 版本号 `ALPHA-20260531 V26.6.0.0-26.6.0.50` [AWMC V2]
<Badge type="tip" text="功能追加" />

- **主题系统支持** — 所有图片路径现在支持主题切换（`prism_plus`/`circle`），主题专属图片从主题子目录读取，共享图片从根目录读取
- **舞/霸者完成表分页** — 舞和霸的完成表分为两页（13+ 及以上 / 13 及以下），生成 `舞-1.png` 和 `舞-2.png`，霸者共用舞的文件
- **底力分析图表视觉优化** — 配色改为温暖夕阳色系，雷达图添加阴影/光晕/顶点高光，条形图添加阴影/高光/背景框，字号增大
- **曲绘查找逻辑优化** — 支持四位数格式（`% 10000`）和 JPG 格式，提高封面查找成功率

<Badge type="danger" text="修复" />

- **版本更新后图片路径全部失效** — 旧路径直接从 `pic/` 根目录读取，新版本需要区分主题目录和根目录
- **牌子/图标文件名更新** — `UI_Plate_300501` → `UI_Plate_550101`，`UI_Icon_309503` → `UI_Icon_509506`
- **文件名连字符改为下划线** — `d-{num}` → `d_{num}`，`t-{num}` → `t_{num}`，`ra-dx` → `ra_dx`，`info-{cat}` → `info_{cat}`
- **背景图片文件名修正** — `song_bg` → `chart_info`，`info_bg` → `play_info`，`unfinished_bg` → `unfinished_1`，`complete_bg` → `complete_1`，`unfinished_bg_2` → `unfinished_2`，`complete_bg_2` → `complete_2`，`rating_bg` → `complete`，`plate_num` → `plate_progress`
- **minfo/info 渲染坐标错位** — 对照 beta 分支修正所有坐标：封面大小/位置、类型图标位置、BPM 位置、定数/notes/谱师/rating 布局全部重排
- **舞/霸牌子查询 KeyError** — `version_map` 中舞和霸没有 `plate_to_dx_version` 的键，改用显式检查避免 KeyError
- **完成表目录结构** — 从 `platedir/plate_{version}.png` 改为独立目录 `plate_table/{version}.png`，牌子图标素材从 `plate_version/{version}{plan}.png` 读取
- **霸者查询路径** — 霸和舞共用 `舞-{page}.png` 完成表文件
- **共享图片 vs 主题图片区分** — `UI_CMN_TabTitle_NewSong`、`{version}`、`{type}`、`d_{num}`、`fcfs`、`UI_CHR_PlayBonus_*`、`UI_GAM_Gauge_DXScoreIcon_*`、`UI_MSS_*` 等共享图片改用 `pic()` 从根目录读取；`logo`、`ra_dx`、`UI_TTR_Rank_*` 等主题图片继续用 `_rtp()` 从主题目录读取
- **完成表渲染逻辑重写** — 对照 beta 分支重写：网格布局改为 `start_x=180, start_y=490, gap=96, row_count=12`，图标大小调整，统计信息位置调整，舞/霸使用 `plate_progress_wu.png`

### 2026/05/30
AWMC 版本号 `ALPHA-20260530 V26.6.0` [AWMC V2]
<Badge type="tip" text="功能追加" />

- Bot 支持落雪查分器

### 2026/05/27
AWMC 版本号 `RELEASE-20260527 V26.5.19`
<Badge type="tip" text="功能追加" />

- 新增 `wife` 表情包生成功能

<Badge type="danger" text="修复" />

- 修复定数错误与浮点数精度问题
- 修复宴会场被统计入 B50 的问题
- 修复其他宴会场相关问题

AWMC 版本号 `RELEASE-20260527 V26.5.18`
<Badge type="tip" text="功能追加" />

- 新增 `dx2026b35` — 支持查询新版本 PRiSM 的 B35 信息

<Badge type="danger" text="修复" />

- 修复定数异常
- 修复宴会场被统计到 `dx2026b50` 中的问题

AWMC 版本号 `BETA-20260527 V26.5.17`
<Badge type="tip" text="功能追加" />

- 新增 `dx2026b35` — 支持查询新版本 PRiSM 的 B35 信息

### 2026/05/25
AWMC 版本号 `RELEASE-20260525 V26.5.16`
<Badge type="tip" text="功能追加" />

- `koishi-plugin-maibot` 1.9.9
  - 防小号绕过冷却机制：绑定时检查同 maiUid 是否已被其他 Bot 用户绑定，警告但允许继续（持有 SGID 证明所有权）
  - 冷却共享：同一游戏账号的所有 Bot 用户共享冷却计时（新增 `maiuid:<maiUid>` 共享键）
  - 超过 2 个绑定时拦截有冷却的命令，提示用户解绑多余账号

<Badge type="danger" text="修复" />

- `koishi-plugin-maibot` 1.9.8
  - 修复 `maibypass` 失效问题：补全 `koishi:<aid>` 统一键，能正确清除以统一键存储的冷却记录
  - `/mai管理员设置个人优先` 参数可省略，缺失时进入交互式（依次询问目标用户、时长）
  - `/mai管理员设置群组优先` 参数可省略，缺失时进入交互式（群内可发 `0` 使用当前群）

### 2026/05/24
AWMC 版本号 `BETA-20260524 V26.5.15`
<Badge type="danger" text="修复" />

- 修复 b50 锐评调用生成类 AI 不会审核输出内容

### 2026/05/23
AWMC 版本号 `RELEASE-20260523 V26.5.13`
<Badge type="tip" text="功能追加" />

- API 新增端点 `/v1/delete_score_manual`
- Bot 新增删除成绩功能

AWMC 版本号 `BETA-20260523 V26.5.14`
<Badge type="tip" text="功能追加" />

- 新增 PC (Play Count) 游玩次数统计功能
  - `更新pc数` — 引导扫码拉取游玩次数数据
  - `pc50` — B50 按游玩次数排序，卡片右下角显示 pc:xx
  - `pca50` — 无视 B35/B15 分组的紧凑版 PC 排序
  - `我的pc数` — 总游玩次数 + 最多的 15 首
  - `pc数 <歌名/ID>` — 查询某首歌游玩次数
  - `pc排行` — 群内用户按总游玩次数排名

AWMC 版本号 `BETA-20260523 V26.5.13`
<Badge type="danger" text="修复" />

- 暂时关闭舞里程功能
- 修复发票 API

### 2026/05/22
AWMC 版本号 `BETA-20260522-C2 V26.5.13`
<Badge type="danger" text="修复" />

- 修复机器人问题

AWMC 版本号 `BETA-20260522 V26.5.12`
<Badge type="tip" text="功能追加" />

- 新增 `mai上传乐曲成绩` 功能
- 新增 `mai获取收藏品` 功能

### 2026/05/20
AWMC 版本号 `RELEASE-20260520 V26.5.10`
<Badge type="warning" text="优化" />

- 服务器升级为物理机，提供极致的性能体验
- 修复了部分已知问题

- 你好我也想要个对象求分配。

### 2026/05/19
AWMC 版本号 `RELEASE-20260519 V26.5.7`
<Badge type="warning" text="变更通知" />

- Wiki、站点配置与开发者文档中的**对外示例链接**，统一展示为 **`awmc.cc` 及其子域**（如 `api.awmc.cc`、`status.awmc.cc`、`store.awmc.cc`、`wiki.awmc.cc` 等）。
- **兼容说明**：若你的书签或自建集成使用过其它域名后缀，请以 **`*.awmc.cc`** 为准并及时更新。


### 2026/05/17
AWMC 版本号 `RELEASE-20260517 V26.5.6`
<Badge type="tip" text="功能追加" />

- 新增了**520**这个特殊的暧昧的小情侣的对于神秘歌曲**Love You**的游玩次数的统计 （你好我也想要个对象求分配。）
- 新增域名 `wmc.pub`。我们做了个神秘的东西。
  

### 2026/05/14
AWMC 版本号 `BETA-20260514 V26.5.5`
<Badge type="tip" text="功能追加" />

- 支持了不同等级和难度的b50查询 比如 `13b50` `白13b50`等。
- 完全测试了 今日吃分推荐 根据最近的数据自动推荐。本功能会根据您的游玩自动推荐较为合适的铺面。
- 支持锐评你的b50，触发指令`锐评一下`。

<Badge type="danger" text="修复" />

- 修复了部分KALEIDXSCOPE网页中的问题
- 修复了KOOK机器人掉线的问题。
- 修复了由于DNS污染跳转到黄色网站的问题。
- 修复了网页加载异常卡顿问题。
- 修复API站点无法登陆的问题。
- 修复STAT无法获取数据的问题。

<Badge type="warning" text="优化" />

- 优化部分情况下使用发票指令造成的小黑屋。
- 优化生成体验和交互。
- Status页面移除了对NET的检测。

## 2026 年 4 月

### 2026/04/04
AWMC 版本号 `RELEASE-20260403 V26.4.03`
<Badge type="danger" text="修复" />

- 修复了QQ机器人无法渲染图片的问题。
- 修复了由于容器化导致的机器人协议端数据丢失。
- 修复了“我在群里有多菜”功能

### 2026/04/03
AWMC 版本号 `RELEASE-20260403 V26.4.03`
<Badge type="danger" text="修复" />

- 修复了商城支付接口出错的问题。
- 修复了KOOK机器人掉线的问题。
- 修复KALEIDXSCOPE页面上的数据的问题。

<Badge type="warning" text="优化" />

- 优化了渲染速度。
- 优化部分情况下使用发票指令造成的小黑屋。
