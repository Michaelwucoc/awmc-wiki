# AWMC BOT 指令帮助

::: tip 提示
善用搜索快捷键 <kbd>/</kbd>（电脑端）或顶部搜索按钮（手机端）进行查询。
:::
::: tip 论坛账号绑定（qbind）
将查分 QQ 与 AWMC 论坛账号绑定的完整流程，详见 [使用说明 · 论坛账号绑定（qbind）](/guide/bot/intro)。
:::

账号绑定、B50 上传、票券、PC 同步、成绩查询与群互动现已合并到同一个 QueryBot。
指令前缀由平台配置决定；下文以不带前缀为例。

---

## 一、账号、上传与 BREAK

开始前请先发送 `用户协议`，打开网页阅读并完整复制确认词。二维码相关指令支持
`SGWCMAID...`、Wahlap `img/MAID...png` 与 `req/MAID...html` 官方链接。

### 1.1 帮助与信息

| 命令 | 说明 |
|------|------|
| `mai账号` | 查看账号功能帮助 |
| `mai状态` / `mymai` | 查看完整账号状态；二维码缓存失效时交互刷新 |
| `舞萌状态` / `mais` | 「服务器失败率」折线图（账号操作全量：发票、maiu 上传、绑定等；`returnCode=0` 计失败；无数据时段省略）+ 服务器实时状态 |
| `maiping` | 测试 AWMC API 连接 |
| `mai地图` | 查询账号游玩过的省份/地区（服务支持时） |
| `mai查询opt <版本号>` | 查询 Mai2 选项文件下载地址，如 `mai查询opt 1.40` |
| `成绩趋势 [天数]` | 查看 AWMC NET. 服务端 Rating 趋势，默认 30 天（别名 `rating趋势` / `mai趋势` / `awmc趋势`） |
| `公告列表` | 查看最近 10 条公告（普通用户不显示内部公告 ID） |
| `确认阅读公告` | 确认当前必读公告，确认同一版本后不再重复提示 |
| `门攻略 [门名]` / `哪些门` | 列出或获取 Kaleidx 门攻略链接；直接发送蓝/白/紫/黑/黄/红门名也会触发 |
| `怎么用` | 消息中包含"怎么用"时自动返回本指令帮助（与帮助入口等效） |

### 1.2 账号绑定

| 命令 | 说明 |
|------|------|
| `mai绑定` / `maibind` | 绑定或认领舞萌 DX 账号；无参数时交互式引导 |
| `mai解绑` | 解绑舞萌账号，保留水鱼/落雪上传配置 |
| `更新pc数` | 使用有效缓存或最新二维码同步 PC 数据 |
| `不同意共享我的数据` | 关闭脱敏成绩 / Rating / 趋势的公开共享（默认开启） |
| `同意共享我的数据` | 重新开启数据共享 |
| `数据共享状态` | 查看当前是否共享脱敏数据 |

### 1.3 水鱼 B50

| 命令 | 说明 |
|------|------|
| `mai绑定水鱼 <Token>` / `maibindfish <Token>` | 绑定水鱼 Token |
| `mai解绑水鱼` | 解绑水鱼 Token，舞萌账号绑定保留 |
| `maiu [二维码或链接]` | 上传 B50 到水鱼 |
| `maiua [二维码或链接]` | 同时上传 B50 到水鱼和落雪 |

### 1.4 落雪 B50

| 命令 | 说明 |
|------|------|
| `lxbind` | 通过 OAuth 绑定落雪（推荐，无需提交导入 Token） |
| `lxunbind` | 解绑落雪 OAuth |
| `mai绑定落雪 <导入Token>` / `maibindlx <导入Token>` | 兼容方式绑定落雪导入 Token |
| `mai解绑落雪` | 解绑落雪兼容 Token |
| `maiul [二维码或链接]` | 上传 B50 到落雪，优先使用 OAuth |

### 1.5 BREAK 与票券

::: danger 警告
发票有风险，操作需谨慎。AWMC TEAM 对发票可能造成的负面影响概不负责。

参见 [AWMC BOT 服务协议与隐私政策](/guide/bot/terms#十、免责声明) 第十条。
:::

| 命令 | 说明 |
|------|------|
| `AWMC签到` / `签到` | 每日签到获取 BREAK |
| `AWMC补签` / `补签` | 补昨天签到；每月最多 3 次，依次消耗 30 / 60 / 90 BREAK，不补发奖励 |
| `AWMC帮助` / `BREAK帮助` | 查看 BREAK 系统说明 |
| `我的AWMC` | 查看 BREAK、连续签到和使用统计；含发票成功/失败率与 `returnCode=0`、null/未返回次数；群内自动附带本群猜歌数据图 |
| `BREAK统计` / `BREAK收支` | 查看所有用户今日、近 7 天、近 30 天 BREAK 收入、支出与净变化 |
| `转账BREAK @用户 数量` | 向其他用户转账 BREAK |
| `BREAK抽奖 [1-10]` | 默认每次消耗 2 BREAK |
| `BREAK抽奖 帮助` | 查看奖池、概率、消耗和示例 |
| `发红包 [总额] [份数]` | 群内发送 BREAK 手气红包（别名 `BREAK红包`）；也可只发 `发红包` 按提示输入 |
| `抢红包` | 领取本群当前红包，每人限一次（别名 `领红包`） |
| `红包状态` | 查看红包领取明细（别名 `红包记录` / `查看红包`） |
| `收回红包` | 发出满 90 秒可主动收回，未领余额退回（别名 `重置红包`） |
| `兑换卡密 [码]` | 兑换 BREAK / 双倍 / FREEDOM 卡密，支持群内与私聊自动兑换（别名 `redeem` / `兑换码`） |
| `我的卡密` | 查看当前生效的卡密加成（别名 `卡密状态` / `我的加成`） |
| `发票` / `fp <2/3/5>` | 请求 2、3 或 5 倍票；每日首次成功免费，之后倍率 × 10 BREAK（20 / 30 / 50） |
| `mai查票` / `查票` | 查询有效票券；按票种、库存及有效期格式化展示，不显示账号 UID |

### 1.6 账号高级操作（计费 / 高风险）

::: danger 警告
`mai改成绩`、`mai删成绩`、`mai改道具` 会直接改动舞萌账号数据，其中
`mai改道具` 未经实际账号测试、可能造成不可逆数据异常，风险由用户自行承担。
参见 [AWMC BOT 服务协议与隐私政策](/guide/bot/terms#十、免责声明) 第十条。
:::

| 命令 | 说明 |
|------|------|
| `mai预览` | 查询账号预览信息（成功查询 5 BREAK，失败不扣费） |
| `mai道具` | 查询账号道具列表（成功查询 5 BREAK） |
| `mai门状态` / `mai查门` / `查门` | 只读展示 Kaleidx Gate 的发现、钥匙与通关状态（成功查询 5 BREAK） |
| `mai改成绩 [歌曲 难度 达成率 DX分 FC FS]` | 交互或一步编辑成绩；DX 分填 0～5 为星级简单模式，填实际分自动切专业模式；成功 75 BREAK / 条 |
| `mai删成绩 [歌曲 难度]` | 交互或一步删除成绩；成功 50 BREAK / 条 |
| `mai改道具 [itemKind itemId add/del]` | 高风险道具修改；成功扣 100 BREAK；Bot 不开放批量 upsert-all |

---

## 二、查询与群互动

> 以下收录查询 Bot 普通用户指令；不含管理员指令。高级变体、数据分析与群管功能见 [2.11 高级功能](#211-高级功能)。

### 2.1 基础

| 指令 | 说明 |
|------|------|
| `帮助maimaiDX` | 查看帮助图 |
| `项目地址maimaiDX` | 查看项目信息 |
| `今日mai` | 今日运势与推荐曲目 |
| `主题` | 查看或切换成绩图主题 |
| `查看排名` | 查看全服 Rating 排行 |
| `我的排名` | 查看自己的 Rating 排名 |

### 2.2 曲目查询

| 指令 | 说明 |
|------|------|
| `查歌 <关键词>` | 按曲名 / 别名搜索 |
| `定数查歌 <定数>` | 按定数搜索 |
| `bpm查歌 <bpm>` | 按 BPM 搜索 |
| `曲师查歌 <曲师>` | 按曲师搜索 |
| `谱师查歌 <谱师>` | 按谱师搜索 |
| `<别名>是什么歌` | 通过别名查曲目 |
| `id <歌曲ID>` | 查看歌曲详情 |
| `谱面<ID><难度>` | 谱面预览，难度：绿 / 黄 / 红 / 紫 / 白 |
| `提取曲绘 <歌曲ID>` | 提取曲绘图片 |
| `mai什么` | 随机推荐一首曲目 |
| `mai什么推分` | 随机推荐适合上分的曲目 |
| `来个<等级>` | 随机推荐指定等级曲目 |
| `来个dx<等级>` / `来个sd<等级>` | 随机 DX / 标准曲 |
| `来个<难度><等级>` | 随机指定难度与等级，如 `来个紫14+` |

### 2.3 别名

| 指令 | 说明 |
|------|------|
| `<曲名>有什么别名` | 查看曲目别名 |
| `id<歌曲ID>有什么别名` | 按 ID 查看别名 |
| `添加别名 <ID> <别名>` | 申请为曲目添加别名 |
| `添加本地别名 <ID> <别名>` | 添加仅本 Bot 生效的本地别名 |
| `同意别名 <Tag>` | 对别名申请投票 |
| `当前投票` | 查看进行中的别名投票 |

### 2.4 成绩查询

| 指令 | 说明 |
|------|------|
| `b50` | 查询 B50 成绩图 |
| `ab50` | 查询全部最佳 50 首 |
| `刷新b50` | 强制刷新成绩缓存 |
| `合作b50` | 两人合作 B50 对比 |
| `合作a50` | 两人合作全部最佳 50 对比 |
| `我有多菜` | Rating 对比图 |
| `我在群里有多菜` | 群内 Rating 对比 |
| `minfo` | 查询个人游玩信息 |
| `ginfo` | 查询群组游玩信息 |
| `分数线` | 查询分数线 |
| `友人对战` | 群内随机友人对战（可加数字收紧 Rating 差，如 `友人对战 300`） |
| `友人对战排行` | 群内友人对战战绩排行（别名 `友人排行` / `友人对战排名`） |
| `b50鸟率` / `B50鸟率` / `b50鳥率` | 查看 B50 鸟率统计（图片） |
| `b50鸟加率` / `B50鸟加率` / `b50鳥加率` / `b50鸟+率` | 查看 B50 鸟加率统计（图片） |

#### 群单曲排名

| 指令 | 说明 |
|------|------|
| `<曲名>排名` | 群内该曲成绩排名 |
| `我的<曲名>排名` | 自己在群内的该曲排名 |
| `<曲名>排名 <N>` | 查看前 N 名 |

支持在曲名前加难度，如 `白潘排名`、`我的白潘排名`。

### 2.5 定数表 / 完成表 / 进度

| 指令 | 说明 |
|------|------|
| `<等级>定数表` | 等级定数表，如 `13+定数表` |
| `<等级>完成表` | 等级完成表，如 `13+ap完成表` |
| `<牌子>完成表` | 牌子完成表，如 `晓极完成表` |
| `<牌子>进度` | 牌子进度，如 `晓极进度` |
| `<等级><达成>进度` | 等级进度，如 `13+sss进度` |
| `<等级><牌子>进度` | 等级牌子进度，如 `13将进度`、`14+极进度` |
| `段位表 [段位]` | 段位认定课题图；默认 PRiSM PLUS，如 `段位表 真二段`，可 @ 用户 |
| `<段位>段位表` | 段位认定课题图的反向写法，如 `真二段段位表` |
| `我要上<分数>分` | 吃分推荐 |
| `我要在<等级>上<分数>分` | 指定等级的吃分推荐 |
| `<等级>分数列表` | 该等级分数列表 |

达成标记支持：`ap` `fc` `fcp` `fs` `fsp` `ss` `sss` `sync` 等。

::: tip 段位表样本
段位认定图会展示 LIFE 规则、课题曲等级/定数、个人最高达成率、匿名样本量、均值、中位数、P25-P75 分布和 SSS 率。样本来自服务器近期已同步且未关闭数据共享的成绩；低样本、冷启动或聚合失败时会回退到内置样本。页脚会标明数据来源，段位通过与否仍以机台结算为准。
:::

### 2.6 猜歌

::: info 私聊触发小游戏
如需游玩猜歌、猜谱面等群游戏，请在私聊中按提示添加群聊 [AWMC 官方群](https://qm.qq.com/q/ZIJWblUc4E) 后再发起。
:::

| 指令 | 说明 |
|------|------|
| `猜歌` | 文字猜歌 |
| `猜曲绘` / `猜曲绘1`～`猜曲绘4` | 曲绘猜歌，可指定 1～4 级难度 |
| `猜曲子` | 音频猜歌 |
| `猜铺面` / `猜谱面` | 谱面视频猜歌（前段静音、曲末带 BGM；带正解音与倒计时） |
| `猜Rating` | 随机选择 1～5 级难度，60 秒内猜匿名 B50 的总 Rating |
| `猜Rating1`～`猜Rating5` | 指定难度；数字越大，展示卡片和辅助信息越少 |
| `猜Rating3 90` | 指定 3 级难度，并把作答时间改为 90 秒（可设 10～300 秒） |
| `重置猜rating` | 强制结束当前猜 Rating 对局 |
| `找内鬼` / `找假卡` | 从 5 张 B50 卡片中找出单曲 RA 被篡改的一张 |
| `重置找内鬼` | 强制结束当前 B50 找内鬼对局 |
| `舞萌极限二选一` / `极限二选一` / `二选一` | 参加 5 轮左右曲目比较的极限二选一；每轮发送 `左` 或 `右` 作答 |
| `加入` / `参赛` | 加入当前极限二选一对局 |
| `重置二选一` | 强制结束当前极限二选一对局 |
| `你想我猜` / `20问猜曲` | Milk 心里想一首曲目，群友用「是/否」提问缩小范围并猜曲名；问得越少分越高，可叠加连击与加倍卡 |
| `重置你想我猜` / `重置20问` | 强制结束当前你想我猜对局 |
| `重置猜歌` | 强制结束当前文字猜歌对局（群内） |
| `查看已有信息` / `20问进度` / `提问列表` | 查看本局你想我猜的问答记录 |
| `舞萌开字母` / `开字母` | 多曲标题开字母看板（默认 8 首）；对局中直接发字母或别名，也可用 `开歌`；通关后计时结算 |
| `开字母排行` / `开字母积分榜` | 本群开字母结算积分榜（图片） |
| `开字母贡献榜` | 本群开字母贡献权重榜（图片） |
| `开字母时间榜` | 本群最佳通关用时榜（图片，含头像） |
| `开字母文字模式` / `文字模式` | 当前或下一局开字母使用纯文字看板 |
| `开字母图片模式` / `图片模式` | 当前或下一局开字母使用图片看板 |
| `开字母自动模式` / `自动模式` | 恢复按人数和负载自动选择开字母看板模式 |
| `查加倍卡` | 查看自己的加倍卡 |
| `猜歌积分排行` | 总积分排行 |
| `本群猜歌排行` | 查看本群猜歌参与与积分排行 |
| `猜歌积分日榜` / `周榜` / `月榜` / `年榜` / `赛季榜` | 各周期积分榜 |
| `猜歌历史日榜` / `周榜` / `月榜` / `年榜` / `赛季榜` | 历史周期榜 |
| `我的猜歌` / `猜歌数据` / `猜歌统计` | 个人猜歌数据图：九玩法近 30 日趋势 + 积分/次数占比环图 + 各玩法卡片与近期明细；可 `我的猜歌 @某人` 查对方 |

::: tip BREAK获取
小游戏每日 BREAK 上限：全局合计 40 / 天；单游戏 猜歌/猜曲绘/猜曲子/猜铺面/你想我猜 各 20，开字母/猜Rating/找内鬼 各 15，极限二选一 10。单游戏到顶后该游戏不再发，全局到顶后所有小游戏不再发，次日 0 点重置；双倍BREAK卡生效期间翻倍发放并豁免上述上限。
:::

::: tip 我的猜歌数据
- 九玩法：猜歌 / 猜曲绘 / 猜曲子 / 猜铺面 / 开字母 / 猜 Rating / 找内鬼 / 二选一 / 你想我猜
- 图内含：近 30 日趋势、自适应玩法卡片、积分/次数占比环图、近期明细；结算后实时更新
- `@某人` 可看对方本群数据，例如 `我的猜歌 @张三`；无 at 仍查自己
- 群内发送 `我的AWMC` 会自动附带本群猜歌数据图（失败不影响账号状态主流程）
:::

::: tip 舞萌开字母
需先由群管发送 `开启mai猜歌`；与猜歌 / 曲绘 / 曲子 / 铺面**同群互斥**。

- 对局中直接发单个字母（如 `m`）开字符，或发曲名/别名猜中；也可 `开字母 m`、`开歌 xxx`
- 字母补齐完整标题时记入补齐者贡献（✅ 字母补齐）
- **局内不计分、不发 BREAK**；全部解开后按**用时星级 + 个人贡献**统一结算
- 用时精确到毫秒，展示为 `xx.xxx秒`
- **星级（自适应）**：默认 ≤30 / 45 / 60 / 90 / 180 秒对应 5～1 星；群内最近通关变快后，按历史用时 P35 收紧五星上限（最低 15 秒、最高仍 30 秒），其它星按 1 : 1.5 : 2 : 3 : 6 相对五星缩放；更慢为 0 星最低奖池。结算文案会显示本局生效阈值
- **贡献权重**：有效开字母 ×1、补齐曲 ×3、开歌 ×4；无贡献不得分；奖池按权重分配
- **限时×3**：活动一周内结算分数与 BREAK 三倍，文案/榜图标注「限时×3」；过期自动恢复 1 倍
- 通关结算顺序：① 猜中/补齐文案 + 用时星级 + 奖池 ② 本局榜单·贡献分成图 ③ 全开看板图；亦可随时查 `开字母排行` / `开字母贡献榜` / `开字母时间榜`
- **人多文字模式**：贡献人数或短时突发达阈值时，局中看板降级为纯文字；通关结算仍强制出图
- 非高峰另有 **2.5 秒/人**冷却（与下方猜歌全局限频独立）；进入文字模式后跳过
- `不玩了` / `结束开字母`：结束并揭晓剩余，**不发**速度奖与贡献奖（不用通关奖池文案）
:::

::: warning 答题限流
同一用户全局 **2.5 秒**只能提交一次答案（猜歌 / 曲绘 / 曲子 / 铺面 / 猜 Rating / 找内鬼）。超限时提示：

`嘿嘿，你的答案被我吃掉啦！(x.x秒后才能发送新的答案）`
:::

::: tip 猜 Rating
需先由群管发送 `开启mai猜歌`；与其它猜歌玩法及开字母 **同群互斥**。

- 随机选一位群友 → 按难度隐藏其 B50 身份和成绩 → 60 秒内发数字猜 Rating → 可修改答案 → 最接近者获胜
- `猜Rating` 随机难度；`猜Rating1`～`猜Rating5` 指定难度；可在后面附加 10～300 秒作答时间，例如 `猜Rating3 90`
- 被抽中的题主不能提交有效答案，也不会进入排名或获得奖励
- 结算：倒计时结束 → 揭晓真实 Rating + 昵称 → 发放积分 / BREAK → 展示完整 B50 图

**分级条件与前三名奖励**：

| 难度 | 展示/隐藏 | 🥇 | 🥈 | 🥉 |
|------|-----------|----|----|----|
| 1 | 20 张；保留成绩等级、FC/FS | 15分 + 2 BREAK | 5分 + 1 BREAK | 3分 |
| 2 | 16 张；隐藏 FC/FS | 18分 + 2 BREAK | 6分 + 1 BREAK | 4分 |
| 3 | 12 张；再隐藏成绩等级 | 21分 + 2 BREAK | 7分 + 1 BREAK | 5分 |
| 4 | 8 张；仅保留最少识别信息 | 24分 + 2 BREAK | 8分 + 1 BREAK | 6分 |
| 5 | 4 张；几乎无辅助信息 | 30分 + 2 BREAK | 10分 + 1 BREAK | 7分 |

其余有效参与者固定获得 1 积分。BREAK 奖励与难度无关：冠/亚固定 2 / 1 BREAK，季军仅得积分；有效参与（不含题主）满 3 人且冠军误差 ≤ 200 时才发放。
:::

::: tip B50 找内鬼
需先由群管发送 `开启mai猜歌`；与其它猜歌玩法及开字母 **同群互斥**。

- 系统随机抽取一位群友的 5 张 B50 卡片，并把其中一张的单曲 RA 改成错误值
- 45 秒内发送 `1`～`5` 作答，可以修改；速度排名按最终答案的提交时间计算
- 被抽中的题主不能提交有效答案，也不会进入奖励名单
- 答对第一名获得 10 积分 + 2 BREAK，第二名 6 积分 + 1 BREAK，第三名及后续答对者 3 积分
- 结算时标出内鬼卡片，并展示真实 RA 与被篡改后的 RA
:::

::: tip 极限二选一
需先由群管发送 `开启mai猜歌`；发 `极限二选一` 开局后，发送 `加入` 或 `参赛` 报名。

- 共 5 轮，每轮比较左右两首歌曲的随机指标，参赛者发送 `左` 或 `右` 作答
- 每轮按正确与否累计分数，结束时统一结算并展示揭晓图
- 揭晓图会在谱面定数旁明确标注：绿 Basic、黄 Advanced、红 Expert、紫 Master、白 Re:MASTER
- `重置二选一` 可由管理员或发起者强制结束当前对局
:::

::: tip 你想我猜（20 问猜曲）
需先由群管发送 `开启mai猜歌`；与其它猜歌玩法及开字母 **同群互斥**。

- 发送 `你想我猜` / `20问猜曲` 开局，Milk 心里想一首舞萌曲目
- 群友直接发送「是/否」类问题（如「是不是紫谱」「定数大于 14 吗」），由 LLM 理解并回答；也支持比较词、区间与分类/谱师/版本等条件
- 可随时直接猜曲名；输入 `查看已有信息` / `20问进度` 查看本局问答记录，`重置你想我猜` 强制结束
- 猜错不结束、超时公布答案；问得越少分越高，积分可叠加连击与加倍卡
:::

### 2.7 PC 数

| 指令 | 说明 |
|------|------|
| `更新pc数` | 绑定机台二维码并同步 PC 数；完成后会提示可用指令 |
| `我的pc数` | 查看个人 PC 数统计（总次数 + 游玩最多的 15 首） |
| `pc排行` / `pc全部排行` / `PC全部排行` | 全部已同步用户的 PC 总量排行（文字列表，标题为「PC全部排行（共 N 人）」） |
| `pc数 <歌曲>` | 查看指定曲目 PC 数 |
| `pc50` | B50 按游玩次数排序（卡片右下角显示 `pc:xx`） |
| `pca50` | B50 内按 PC 排序（Rating B50 的 50 首重排，输出图片） |
| `游玩排行50` / `游玩PC50` / `PC游玩50` / `pc游玩50` | 全库游玩最多的 50 首谱面（不限于 Rating B50，输出图片） |

#### 命令对比

| 命令 | 作用 |
|------|------|
| `游玩排行50` | 全库游玩最多的 50 首（输出图片） |
| `pca50` | 你 Rating B50 内的 50 首，按 PC 重排（输出图片） |
| `pc排行` | 全用户 PC 总量排行（输出文字） |

::: info 说明
`游玩排行50` 为避免与 `pc排行` 前缀冲突，未命名为 `pc排行50`。例如 `sølips`（137 次）、`Believe the Rainbow`（120 次）等高频曲目会出现在游玩排行图中，即使不在你的 B50 内。
:::

### 2.8 数据源与落雪查分器

| 指令 | 说明 |
|------|------|
| `数据源` | 查看当前数据源（新用户默认为 AWMCNET） |
| `数据源 AWMCNET` / `数据源 水鱼` / `数据源 落雪` | 切换个人数据源 |
| `成绩趋势 [天数]` | 查看 AWMC NET. 服务端 Rating 趋势（别名 `rating趋势` / `mai趋势` / `awmc趋势`） |
| `lxbind` | 绑定落雪查分器（OAuth） |
| `lxunbind` | 解绑落雪查分器 |
| `lxb50` | 使用落雪数据源查询 B50 |

::: info 说明
AWMC NET. 是默认成绩库，发送一次 `SGWCMAID` 即可查分，无需绑定水鱼/落雪。
B50 系列按用户偏好自动走 AWMCNET/水鱼/落雪；部分功能（拟合、含金量等）落雪不支持时会回退水鱼。
:::

### 2.9 谱面印象

| 指令 | 说明 |
|------|------|
| `谱面印象 <曲目>` | 查看谱面印象 |
| `写谱面印象 <曲目> ` | 上传谱面印象 |
| `谱面排行` / `谱面热度` | 查看谱面印象热度排行 |

::: tip 提示
使用 `查歌`（ID 或别名）查询时，会**自动附带**谱面印象、标签、定数变化、预览链接等合并转发。
:::

### 2.10 AWMC BREAK

| 指令 | 说明 |
|------|------|
| `AWMC签到` | 每日签到获取 BREAK |
| `我的AWMC` | 查看账号状态与使用统计（含发票 returnCode 统计）；群内附带猜歌数据图 |
| `AWMC帮助` | 查看 BREAK 系统说明 |
| `转账BREAK @用户 数量` | 向其他用户转账 BREAK |
| `BREAK抽奖 [1-10]` | 默认每次消耗 2 BREAK，可一次抽 1～10 次 |

::: tip BREAK 食用说明

有修改建议请前往 [AWMC 论坛](https://bbs.wmc.pub/forums/awmc.19/) 提出。接入 AI 后会自动修改机器人并更新。

1. 每日签到可以获得 BREAK，连续签到可以获得更多 BREAK。
2. 锐评按 Token 基础价 ×3 计费，调用前预扣 6 BREAK，成功后多退少补，失败全退。
3. 可以使用 BREAK 抽奖，最多支持 10 连抽。
4. 普通猜歌成功默认获得 1 BREAK；猜 Rating / 找内鬼按本局排名与难度结算。
5. 「今日舞萌」会根据人品值增加 BREAK。
6. 和群主谈情说爱，可能会获得一些 BREAK。

:::

> 查分指令每日首次实际请求查分器 API 免费，之后每次扣 1 BREAK（缓存命中不扣）。
> 普通猜歌每次猜对默认获得 1 BREAK；猜 Rating / 找内鬼按玩法规则结算。成绩上传和发票只在外部操作真正成功后结算，
> 并分别享有每日首次成功免费。

### 2.11 高级功能

以下为查询 Bot 的进阶指令与变体，日常使用可跳过。

#### 成绩图变体

| 命令 | 说明 |
|------|------|
| `紫b50` / `白b50` / `Master b50` 等 | 按谱面难度筛选 B50（紫=Master，白=Re:MASTER） |
| `三星b50` / `四星b50` | 按成绩 DX 星数筛选 B50（分别为 3 星、4 星） |
| `3b50` / `4b50` / `13+b50` | 按等级/定数筛选 B50；数字等级与“三星/四星”星数筛选不是同一条件 |
| `紫13+b50` / `14.0b50` / `13-14b50` | 组合筛选谱面难度与等级/定数；也支持 `Basic`、`Master`、`Re:MASTER` 等英文难度名 |
| `<筛选条件>ab50`（如 `13+ab50`） | 与 `<筛选条件>b50` 相同，但不按 B35/B15 分组，直接从筛选结果中取 Rating 最高的 50 首 |
| `紫ab50` 等 | 按难度筛选 ab50 |
| `镜代b50` 等 | 按版本代筛选 B50 |
| `l镜代b50` / `l爽代b35` 等 | 历代版本定数重算 B50/B35 |
| `dx2026b35` | 彩代 B35 |
| `fcb50` / `fcallb50` | FC 向 B50 |
| `apb50` / `apallb50` | AP 向 B50 |
| `拟合b50` / `拟合b50全部` | 按拟合定数排序 |
| `寸b50` / `寸ab50` | 寸止（踩评级线）成绩 |
| `锁血b50` / `锁血ab50` | 锁血（踩评级下限）成绩 |
| `越级b50` / `越级ab50` | 越级成绩 |
| `理想b50` / `理想ab50` | 评级升一档后的理想 B50 |
| `含金量` / `含水量` | 成绩含金量/含水量分析图 |

#### 分析 / 推分 / 对战

| 主命令 | 别名 | 用法示例 | 说明 |
|--------|------|----------|------|
| `弱项处方` | `弱项处方单` `底力处方` `练习推荐` | `弱项处方` / `弱项处方@某人` | 弱项处方单图片 |
| `b50风险` | `B50风险` `b50风险预警` `风险预警` | `b50风险` / `b50风险@某人` | B50 风险预警图片 |
| `对战战绩` | `headtohead` `h2h` `对决战绩` | `对战战绩@群友` | Head-to-Head 对比图 |
| `目标rating` | `rating沙盘` `目标分` `推分沙盘` | `目标rating 16000` / `目标rating 15500@某人` | 推分沙盘文字方案 |
| `底力分析` | — | `底力分析` | B50 标签雷达图 + 条形图，仅供参考 |
| `锐评一下` | `分析b50` `B50分析` | `锐评一下` | 调用 LLM 对 B50 进行锐评；按实际 Token 计费，调用前预扣 10 BREAK，成功多退少补、失败全退 |

#### 地板

| 命令 | 说明 |
|------|------|
| `地板` | 查看整体 B35/B15 地板 |
| `地板 14+` | 查看 B50 中 14+ 谱面的地板 |
| `地板 紫13` / `地板 master` | 按难度等级 + 定数筛选 |
| `b50地板` / `rating地板` | 同上（别名） |
| `地板 @某人` | 查他人地板 |

#### 中文简称进度

| 指令示例 | 等价于 | 完成条件 |
|----------|--------|----------|
| `13将` / `13将进度` | `13 sss进度` | 达成率 ≥100% |
| `14+极` | `14+ fc进度` | FC 及以上 |
| `13神` | `13 ap进度` | AP 及以上 |
| `13舞舞` | `13 fsd进度` | FSD 及以上 |
| `13者` | `13 bbb进度` | 达成率 ≥80% |

也支持 `13将 未完成`、`13将 2` 等分页筛选，以及 `13将 @某人` 查询他人。

#### 数据存储与报告

::: warning 注意
需先 **开启存储数据**。
:::

| 命令 | 说明 |
|------|------|
| `立即存储数据` | 手动存一份快照 |
| `关闭存储数据` | 关闭自动存储 |
| `存储历史` | 查看历史存档 ID |
| `查看存档 <ID>` | 查看某次快照详情 |
| `日报` / `周报` / `月报` | 近 1/7/30 天成绩报告图 |
| `对比存档 <旧ID> <新ID>` | 两次存档对比报告 |
| `今日吃分推荐` | 基于历史的个性化推分建议（文字） |
| `牌子统计` | 牌子达成统计 |

#### 群聊互动扩展

| 命令 | 说明 |
|------|------|
| `群聊rating排行榜` | 群内 Rating 排行 |
| `群吃分榜` / `群寸止榜` / `群锁血榜` | 群内各类榜单 |
| `开启mai猜歌` / `关闭mai猜歌` | **群管**：开关猜歌 |
| `开启别名推送` / `关闭别名推送` | **群管**：本群别名推送 |

#### 管理员渲染与预制状态

| 命令 | 说明 |
|------|------|
| `猜歌预制状态` / `猜歌预制` / `预制状态` | **管理员**：查看猜曲/猜铺面预制任务状态 |
| `渲染状态` / `查询渲染` / `渲染任务` | **管理员**：查看图片与视频渲染任务进度 |

#### 附：关于「拍一拍」

BOT 对 QQ 的拍一拍会做出反馈。若 BOT 是管理，可能触发随机时长的**禁言**（最高 1 天），拍之前请三思。

---

## 交流与反馈

- **QQ 群**：[1072033605](https://qm.qq.com/q/7157yt6n6w)
- **官网**：https://awmc.cc
